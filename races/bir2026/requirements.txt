# Pinned to reproduce the NB2026 snapshot reference as closely as this platform
# allows. Cross-platform caveat (the snapshot-compare finding, 2026-07-09): 1-ulp
# interpolation differences put ~39 samples exactly on a rounding half-boundary
# vs the snapshot reference — compare_data's --ties mechanism exempts recorded ties;
# everything else must be byte-identical.
# pandas 3.x also works (same output) once its [s]-resolution epoch default is
# neutralized by the adapters' explicit datetime64[ns] cast.
pandas==2.2.3
numpy==1.26.4
pyyaml>=6.0
