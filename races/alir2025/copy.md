# <race> — authored copy

Human-readable frozen record of every narrative block. Its machine form is
`copy.js` (`window.__COPY__`) — author both together; the shell consumes
copy.js at build/runtime. See races/nb2026/{copy.md,copy.js} for the full
slot set: titleblock (eyebrow/h1/sub/result/tzn), app states, per-section
titles+notes, race-chart mode notes (h/e), sog axis hints, controls hint,
pill labels, more-panel strings, empty states, footer.

Voice and claims are the owner's: a session may propose microcopy, never
analysis claims. Copy pins at the stage-3 stop; corrections after that are
recorded stage-3 items.
