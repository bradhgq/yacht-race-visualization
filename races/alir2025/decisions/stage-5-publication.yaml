# stage-5 — Publication Ledger — ALIR 2025
# Tier 1 (public data). Prime rule 4 note: no private inputs ever entered the
# repo — no crew journal, no nav log. The one participant-derived item below
# was given by the owner in-session expressly for this public visualization.
public_items:
  - { item: "events.yaml 'The squall hits Daffodil' — the owner's deck account (source: journal)",
      opted_in: true }   # owner supplied it for the viz, 2026-07-23; corroborated by buoy 44065
  - { item: "events.yaml 'Anchored 175 yards from the line' + Montauk timing — derived from
      Lenoble's PUBLISHED accounts (source: transcript), attributed",
      opted_in: true }   # already public at source; attribution in the event text and footer
  - { item: "seven analysis-sourced events + all copy claims — pipeline/official-record derived",
      opted_in: true }
private_confirmed: true          # nothing else exists to strip: Tier 1, public inputs only
regression: green                # 16/16 under TZ=America/New_York AND TZ=UTC; snapshot IDENTICAL
artifact:
  hash: "sha256:6f5cc3d367abb098814a2880… (dist/standalone.html at this commit; full hash via
    `shasum -a 256 races/alir2025/dist/standalone.html`)"
  url: "PENDING owner deploy. Silverbox nginx location: /daffodil-around-long-island-2025/ under the hgq.fyi
    vhost, aliased to ${inputs.race-viz-site}/races/alir2025/dist/. Deploy = merge the PR, add
    the nginx location, then `nix flake update race-viz-site && sudo nixos-rebuild switch
    --flake .#silverbox` — owner-only actions (CLAUDE.md)."
notes:
  - "dist/ committed via git add -f (the global dist/ ignore would silently drop it — the BIR
    first-deploy trap; build_race.py warned and the warning was heeded)."
  - "og.png generated by scripts/make_og.py from the frozen snapshot (dual-focus card; squall
    count deliberately unasserted — 'a squall line')."
  - "Mobile pass at 375px: no horizontal scroll, 43/43 door dots, narrow band labels. Desktop
    and the split build verified through four stage-4 review rounds."
confirmed_by: "Brad"             # by the 'proceed to stage 5' instruction and this PR's review
date: "2026-07-23"
