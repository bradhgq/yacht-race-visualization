# Pinned to reproduce the frozen NB2026 payload as closely as this platform
# allows. Cross-platform caveat (the GATE-A finding, 2026-07-09): 1-ulp
# interpolation differences put ~39 samples exactly on a rounding half-boundary
# vs the frozen oracle — compare_data's --ties mechanism exempts recorded ties;
# everything else must be byte-identical.
# pandas 3.x also works (same output) once its [s]-resolution epoch default is
# neutralized by the adapters' explicit datetime64[ns] cast.
pandas==2.2.3
numpy==1.26.4
pyyaml>=6.0
