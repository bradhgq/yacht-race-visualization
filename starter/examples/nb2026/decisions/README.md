# decisions/

The worked example predates the checkpoint protocol: its CP-0/2/4/5 decisions
were made in chat and survive only implicitly (shipped copy, RETROSPECTIVE §5,
the golden values in `config.yaml`) — recorded as DOC_GAPS.md #7. Do not
backfill records nobody confirmed.

Records that post-date the protocol land here for real:

- `CP-3-amendment-park-copy.yaml` (2026-07-07) — the three park-copy
  corrections (mis-scoped 18-boat-set values quoted above the displayed
  default-12 table), owner-confirmed. Applied to presentation.js /
  modules/parkfair.js / copy.md; the "Everyone parked" insight event keeps its
  18-set numbers by design (correctly scoped to "the comparison set").
